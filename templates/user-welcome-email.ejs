Hello<%= user.nameFirst ? " "+user.nameFirst : "" %>, welcome to <%= siteName %>! <% if(user.password) { %>You can login at the link below using this email address and the password you specified.<%} else {%>You can login at the link below and create your password.<%}%>

<%= link %>

Sincerely,

The Team at <%= siteName %>
